export type User = {
    id: string
    email: string
    verified_email: number
    name: string
    given_name: string
    family_name: string
    picture: string
    locale: string
    subscription: string
    timestamp: string
}
